apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jenkins-credentials
  namespace: jenkins
  labels:
    app.kubernetes.io/name: jenkins
    app.kubernetes.io/component: credentials
    app.kubernetes.io/managed-by: helmfile
    environment: production
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-secrets
    kind: ClusterSecretStore
  target:
    name: jenkins-credentials
    creationPolicy: Owner
  data:
  - secretKey: admin-user
    remoteRef:
      key: kubernetes/jenkins-credentials
      property: admin-user
  - secretKey: admin-password
    remoteRef:
      key: kubernetes/jenkins-credentials
      property: admin-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-credentials
  namespace: harbor
  labels:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/component: credentials
    app.kubernetes.io/managed-by: helmfile
    environment: production
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-secrets
    kind: ClusterSecretStore
  target:
    name: harbor-credentials
    creationPolicy: Owner
  data:
  - secretKey: HARBOR_ADMIN_PASSWORD
    remoteRef:
      key: kubernetes/harbor-credentials
      property: HARBOR_ADMIN_PASSWORD
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarqube-credentials
  namespace: sonarqube
  labels:
    app.kubernetes.io/name: sonarqube
    app.kubernetes.io/component: credentials
    app.kubernetes.io/managed-by: helmfile
    environment: production
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-secrets
    kind: ClusterSecretStore
  target:
    name: sonarqube-credentials
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: kubernetes/sonarqube-credentials
      property: username
  - secretKey: password
    remoteRef:
      key: kubernetes/sonarqube-credentials
      property: password
  - secretKey: currentPassword
    remoteRef:
      key: kubernetes/sonarqube-credentials
      property: currentPassword
  - secretKey: monitoring-passcode
    remoteRef:
      key: kubernetes/sonarqube-credentials
      property: monitoring-passcode
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-secret
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd
    app.kubernetes.io/component: credentials
    app.kubernetes.io/managed-by: helmfile
    environment: production
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-secrets
    kind: ClusterSecretStore
  target:
    name: argocd-secret
    creationPolicy: Owner
  data:
  - secretKey: admin.password
    remoteRef:
      key: kubernetes/argocd-secret
      property: admin.password
  - secretKey: server.secretkey
    remoteRef:
      key: kubernetes/argocd-secret
      property: server.secretkey